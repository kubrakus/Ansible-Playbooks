    - hosts: all
      gather_facts: yes
      tasks:
      - name: Get service_facts
        service_facts:

      - name: update sysctl param
        sysctl:
          name: test
          value: test
          state: present
          reload: yes
          state: enabled
        when: 
          - ansible_facts.services['jenkins.service'].state == 'running'
